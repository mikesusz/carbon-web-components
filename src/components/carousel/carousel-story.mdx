import { Preview, Props, Story } from '@storybook/addon-docs/blocks';

# Carousel

Carousel manipulates the content shown following an exclusive or “either/or” pattern. It is used to toggle between two or more content sections within the same space on screen. Only one section can be shown at a time.

<Preview withToolbar>
  <Story id="components-carousel--default" height="160px" />
</Preview>

## `<bx-carousel>` attributes, properties and events

<Props of="bx-carousel" />

## `<bx-carousel-item>` attributes and properties

<Props of="bx-carousel-item" />
